---
session: api

cwd: /

before: 
  - ls
stop: 
  - ls

env:
  VAR: env

follow: true

windows:
    - name: "first"
      cwd: "."
      panes:
        - type: horizontal
          commands:
            - ls
            - zsh
        - type: vertical
          commands:
            - ls
            - zsh
