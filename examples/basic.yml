---
session: api

before: 
  - ls
stop: 
  - ls

env:
  VAR: env

follow: true

windows:
    - name: "first"
      cwd: "./"
      panes:
        - type: horizontal
          cwd: "."
          commands:
            - ls
            - zsh
        - type: vertical
          cwd: "."
          commands:
            - ls
            - zsh
    - name: "second"
      cwd: "./"
      panes:
        - type: horizontal
          cwd: "."
          commands:
            - ls
            - zsh
        - type: vertical
          cwd: "."
          commands:
            - ls
            - zsh
    
    
