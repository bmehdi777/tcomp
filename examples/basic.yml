---
session: api

before: 
  - ls
stop: 
  - ls

env:
  VAR: env

windows:
    - name: "first"
      cwd: "./"
      panes:
        - type: horizontal
          cwd: "."
          commands:
            - ls
            - zsh
        - type: horizontal
          cwd: "."
          commands:
            - ls
            - zsh
    
